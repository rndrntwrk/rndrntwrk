name: RNDRNTWRK live-load diagnostic trigger

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  marker:
    runs-on: ubuntu-latest
    steps:
      - run: echo "The PR-scoped base workflow performs the live-site trace."
